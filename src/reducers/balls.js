

const Balls = (state, action) => {
    switch (action.type) {
        case "UPDATE_BALLS":
            return {...state};
        default:
            return {...state};
    }
  };
  
  export default Balls;